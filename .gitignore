# Compiled binaries — released via GitHub Releases, never committed
/ai-ssh-tools
/ai-ssh-tools.exe
/ai-ssh-tools-*
*.exe
/dist/

# SSH credentials config — NEVER commit real credentials
ssh_hosts.json
.server-creds

# Go module cache
vendor/

# IDE & temp files
.vscode/
.idea/
*.swp
*~
*.exe~
